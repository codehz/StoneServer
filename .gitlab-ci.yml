image: base/devel
stages:
  - build

before_script:
  - pacman -Su cmake

job_build:
  before_script:
    - git submodule sync --recursive
    - git submodule update --init --recursive
  script:
    - mkdir build
    - cd build
    - cmake ..
    - make
  stage: build